# Agent 入口

- 请先读取 `.agents/AGENTS.md`。
- 非简单任务按 `.agents/core/harness/task-intake.md` 分级。
- normal / complex / long 任务结束前按 `.agents/core/harness/knowledge-feedback.md` 追加知识 Raw；统一 bridge 自动编译机器 Wiki。
