# 代码审查清单

## 结构

- 使用现有模块边界。
- 没有无关重构。
- 新抽象具备真实复用、生命周期或数据契约价值。

## 注释与字段

- 新函数包含 `@param` 和 `@returns`。
- 新 payload 字段有中文语义注释。
- 注释解释业务含义，而不是重复代码。

## 运行时行为

- 本地状态仍留在本地。
- Pinia 只用于跨视图协调。
- 地图图层有清理路径。
- 弹窗能正确关闭或重置。

## 验证

- 已运行匹配范围的验证。
- 未经用户要求不运行重型 build。
- 已说明未验证项和人工确认项。
