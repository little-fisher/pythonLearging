# 复制为 backend/.env 后填写。不要把真实 .env 提交到 Git。
DEEPSEEK_API_KEY=replace-with-your-key

# 模型名可能随平台升级，请在动手当天以 DeepSeek 官方模型页为准。
DEEPSEEK_MODEL=deepseek-v4-flash

# 前端默认联调地址对应此端口。
APP_HOST=127.0.0.1
APP_PORT=8000

